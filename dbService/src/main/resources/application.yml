spring:
  application:
    name: db-service

server:
  port: 8051

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8050/eureka/
  instance:
    hostname: localhost